"""Shared, importable test helpers (NOT a conftest — plan D6).

Holds the pipeline fakes the e2e/live tests reuse without duplicating the
fixtures in test_pipeline_batch.py or adding a conftest the repo forbids.
"""
