# Secrets & local config
.env
*.env
config.yaml
config.local.yaml

# Runtime data (contains PII; never commit)
data/

# Spike samples (treated as PII; never commit)
spikes/**/golden_set/
spikes/**/data/

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
build/
dist/

# Local tooling / agent runtime state (machine-local; never commit)
.claude/
.context/
